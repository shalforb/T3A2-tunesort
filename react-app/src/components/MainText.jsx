

function MainText({ mainText }) {
    return (
        <div>
            <p className="border flex justify-center p-36 text-[64px] font-serif">{mainText}</p>
        </div>
    );
}

export default MainText;
